import {
  DEVICE_HEIGHT,
  DEVICE_WIDTH,
  SCREEN_HEIGHT,
  SCREEN_WIDTH,
} from "./constants";

import { getScale, isAndroid } from "./functions";

export {
  DEVICE_HEIGHT,
  DEVICE_WIDTH,
  SCREEN_HEIGHT,
  SCREEN_WIDTH,
  getScale,
  isAndroid,
};
